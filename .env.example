# ========================================
# VARIÁVEIS DE AMBIENTE OBRIGATÓRIAS
# ========================================

# Senha do administrador (OBRIGATÓRIA para primeiro acesso)
# Esta senha será usada para criar o usuário admin inicial
# Email do admin: admin@hamburguer.com
# Login Admin: /admin/login
ADMIN_PASSWORD=sua_senha_admin_segura_aqui

# Chave secreta do Flask (recomendada para produção)
SECRET_KEY=sua_chave_secreta_aqui

# ========================================
# VARIÁVEIS OPCIONAIS
# ========================================

# Controle de SocketIO (padrão: true)
# Para economizar recursos no free tier, defina como false
ENABLE_SOCKETIO=true

# Número de workers do Gunicorn (padrão: 2)
# No free tier, não exceda 2 workers
WEB_CONCURRENCY=2

# ========================================
# CHAVES API (OPCIONAIS)
# ========================================

# Integração Mercado Pago (para pagamentos)
MERCADOPAGO_ACCESS_TOKEN=seu_token_mercado_pago_aqui

# Chave API do Gemini (para chatbot)
GEMINI_API_KEY=sua_chave_api_gemini_aqui

# Múltiplas chaves Gemini (para rotação automática)
GEMINI_API_KEY_1=chave-1
GEMINI_API_KEY_2=chave-2
GEMINI_API_KEY_3=chave-3

# ========================================
# CORS (OPCIONAL)
# ========================================

# Origens permitidas para CORS (padrão: *)
# Para produção, adicione as origens permitidas separadas por vírgula
# Exemplo: CORS_ALLOWED_ORIGINS=https://seudominio.com,https://www.seudominio.com
# CORS_ALLOWED_ORIGINS=*

# ========================================
# COMO USAR
# ========================================
# 
# 1. Copie este arquivo para .env:
#    cp .env.example .env
#
# 2. Edite o arquivo .env e adicione suas credenciais
#
# 3. NUNCA commite o arquivo .env no git!
#    (já está no .gitignore)
#
# 4. Para Render/Replit, configure as variáveis no painel
#    da plataforma ao invés de usar arquivo .env
#
